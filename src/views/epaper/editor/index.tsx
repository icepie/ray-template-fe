import { TempEditorComponent } from '@/components'

const Profile = defineComponent({
  name: 'BaseProfile',
  render() {
    return <TempEditorComponent></TempEditorComponent>
  },
})

export default Profile
